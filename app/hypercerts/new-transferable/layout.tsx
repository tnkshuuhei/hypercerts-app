export const metadata = {
  title: "Create a transferable hypercert",
  description: "Create and share your own hypercerts",
};

const HypercertLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default HypercertLayout;
